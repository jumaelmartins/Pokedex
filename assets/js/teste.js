



/*let resultado = [3, 5, 15, 4]



for (let i = 0; i < resultado.length; i++) {
    const element = resultado[i];
    const div3 = element % 3;
    const div5 = element % 5;
    if (div3 + div5 === 0) {
        console.log('FizzBuzz')
    }   else if (div3 === 0) {
        console.log('Fizz')
    }   else if (div5 === 0) {
        console.log('Buzz')
    }   else {
        console.log(element)
    }    
} */
/*let n = 15;

//let lista = [];
let soma = 0
for (let i = 0; i <= n; i++) {
    const numero = i;
    //lista.push(i);
    soma += numero;
}
*/

/*let elementos = [64, 137, -16, 43, 67, 81, -90, 212, 10, 75]; 
let valor = 3;
let valor2 = 0;

for (let i = 0; i < elementos.length; i++) {
    const element = elementos[i];
    if (valor === element) {
        valor2 += valor
        console.log('Achei ' + (valor) + ' na posicao' + ' ' + elementos.indexOf(element))
    }
}

if (valor2 != valor) {
    console.log('Numero ' + (valor) + ' nao encontrado!')
}*/


// offset padrão 0 limit 10.
// criar botão filtro geração